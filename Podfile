# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'iOSLearningApp' do
  use_frameworks!

  # Pods for iOSLearningApp
  # pod 'SDWebImage','5.10.0'
  pod 'AYNetworkManager', :path => 'AYNetworkManager/'
  pod 'AYUserDefaultsManager', :path => 'AYUserDefaultsManager/'

end
